# User Management

## Task

Implement basic CRUD (Create, Read, Update, Delete) operations over a list of users, along with sorting and filtering capabilities.

## Approach

Users are stored in an array of objects. The interface allows adding new users, removing existing ones, and displaying the list sorted (e.g. alphabetically via `localeCompare`) or filtered by a given criterion — all handled through direct DOM manipulation.
